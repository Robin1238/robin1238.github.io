<!-- <a href="/about-me"  aria-disabled="true"> -->
<div class="container">
  <header>
    <p class="grettings">
      ¡Hola! Soy <span class="bold">Robin Andres</span>, un gusto.
    </p>
    <img class="profile" src="/images/Me.jpg" alt="Foto de Robin Andres" />
  </header>

  <p>
    <span class="text">
      <span class="bold">Desarrollador</span> con experiencia en
      <span class="bold">frontend, diseño</span>, desarrollo de
      <span class="bold">APIs y backend</span>. Me enfoco en crear interfaces claras,
      funcionales y con buena experiencia de usuario, sin descuidar la lógica detrás de
      cada proyecto.
    </span>
  </p>

  <p>
    <span class="text">
      Mi formación es principalmente
      <span class="bold">autodidacta</span>, lo que me ha permitido trabajar con
      <span class="bold">bases de datos</span> y distintas tecnologías a lo largo de proyectos
      personales.
    </span>
  </p>

  <p>
    <span class="text">
      Disfruto enfrentar desafíos que requieren
      <span class="bold">creatividad y pensamiento crítico</span>, y sigo creciendo con el
      objetivo de consolidarme como
      <span class="bold">desarrollador fullstack</span>.
    </span>
  </p>
</div>

<!-- </a> -->

<style>
  .container {
    width: 100%;
    height: 100%;
    padding: 1rem 2rem;
    border-radius: 8px;
    border: 3px solid var(--divisor);
    background-color: var(--bg-surface);
    overflow-y: auto;
    scrollbar-width: thin;
    scrollbar-color: var(--divisor) transparent;
    scroll-behavior: smooth;
  }

  .container::-webkit-scrollbar {
    width: 8px;
  }

  .container::-webkit-scrollbar-track {
    background: transparent;
  }

  .container::-webkit-scrollbar-thumb {
    background-color: var(--divisor);
    border-radius: 10px;
    border: 2px solid transparent;
    background-clip: content-box;
  }

  .container::-webkit-scrollbar-thumb:hover {
    background-color: var(--text-primary);
  }

  p {
    margin: 0;
    font-size: clamp(1.2rem, 1vw, 1.5rem);
    font-family: "Satoshi", Verdana, sans-serif;
    letter-spacing: 0.5px;
  }

  .profile {
    width: 70px;
    height: 70px;
    padding: 2px;
    border-radius: 50%;
    border: 3px solid var(--text-color);
    object-fit: cover;
    object-position: top;
  }

  header {
    display: flex;
    margin-bottom: 1.5rem;
    align-items: end;
    justify-content: space-between;
    gap: 20px;
  }

  header .grettings {
    font-size: clamp(1rem, 2vw, 1.5rem);
    opacity: 0.8;
  }

  .text {
    color: var(--text-primary);
    opacity: 0.8;
    margin: 1rem 0;
    display: block;
  }

  .bold {
    font-weight: 900;
  }
</style>
