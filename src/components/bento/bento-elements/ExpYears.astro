<div class="container">
  <p class="text">
    <span id="Dawn">+2</span>
    Años de experiencia
  </p>
</div>

<style>
  .container {
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    background-color: var(--bg-second-surface);
  }

  .text span {
    font-family: "Array", sans-serif;
    display: block;
    font-size: clamp(1.25rem, 10vw, 5.75rem);
  }

  .text {
    font-family: "ABeeZee", Verdana;
    font-size: clamp(1.12rem, 1.25vw, 1.5rem);
    line-height: 1;
    font-weight: 400;
    white-space: nowrap;
    text-align: center;
  }
</style>
